{
    "scripts": {
        "start": "node index.js",
        "build": "cd client && npm install && npm run build"
    },
    "dependencies": {
        "cors": "^2.8.5",
        "express-favicon": "^2.0.1"
    },
    "engines": {
        "node": "12.16.2"
    }
}
